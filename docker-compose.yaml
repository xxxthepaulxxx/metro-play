name: metro-play

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: metro-play-frontend:dev
    ports:
      - "${FRONTEND_PORT:-8000}:80"
      - "5173:80"
    restart: unless-stopped
