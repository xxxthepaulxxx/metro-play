name: metro-play

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: metro-play-frontend:dev
    ports:
      - "${FRONTEND_PORT:-5173}:80"
    restart: unless-stopped
